c=RA /class|def /
asd=RR RA /`c/add/sparse/dense/ RA /def /
dts=RR RA /`c/dense/sparse/flat/ RA /def /
f=RA /def /
sda=RR RA /`f/sparse/dir/app/ RA /def/
aasd=RR RA /`f/_add_sparse_dense/ RA /def sparse_dot/
step=RR RA /def step/ RA /def save/
sda1=FA SAR sda RR RA /if/num/old/ RA /del q/
sda2=FA SAR sda RR RA /del q/ RA /total_norm/
ag=RR RA /`f/_add_grad/ RA /def _dir/
smuls=RR RA /`f/dot/ RA /def add_sparse/
de=RR RA /`f/dir/eval/ RA /def sparse/
init=RR RA /`c/FBFGS/class/ RA /def .numel.self/
ci=RR RA /def _cubic_interpolate/ RA /class FBFGS/
sft=RR RA /class SparseFlatTensor/ RA /def _cubic/
sw=RR RA /def/_strong_wolfe/ RA /torch/jit/
gfg=RR RA /def _gather_flat_grad/ RA /def /
ss=FA SAR step RR RA /SELECTION SUBROUTINE/ RA /dense_to_sparse/
tmp=LR 1280 1290
rew=RR RA /def .rho/ RA /^ *def/
sdtl=RR RA /split/def/direction/layers/ RA /return/
gfng=RR RA /gather/norm/def/ RA /return/
td=RA /TODO/
mn=RR RA /def norm_select\(/ RA /def/gather/flat/
sdtg=RR RA /split/direct/groups/def/ RA /def/
gso=RR RA /def gram_schmidt/ RA /^ *def/
comm=RA /^ *#/
bl=RR RA /def/back/loop/ RA /return/
fl=RR RA /def/forward/update/ RA /return/
stg=RR RA /split_tensor_to_groups_jit/ RA /return/
todo=RA /`comm/TODO/
