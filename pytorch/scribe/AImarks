modeli=RA /model.* =/
coml=RA /^#/
clos=RR RA /def clos/ RA /return output/
main=RR RA /while True:/ RA /#EOF/
init=RR RA /#INIT START/ RA /#INIT END/
imp=RA /import/
initc=FA SAR init RR RA /os.path.exists.filename/ RA /model.train/
conf=RR RA /config/ RA /def calculate/
opt=RA /optimizer =/
